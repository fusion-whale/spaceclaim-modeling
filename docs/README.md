# docs/ · 图与汇报页

| 文件 | 内容 |
|---|---|
| `pche.svg` | PCHE 一对冷热通道：截面（D 形、平边朝 +z、上下对齐）+ 轴测 + 实测数据 |
| `fuel-assembly-5x5.svg` | 5×5 燃料组件流动通道：75×75 截面 + 轴测 + 水力学量（Dh = 13.4937 mm） |
| `shell-and-tube.svg` | 管壳式换热器：纵剖面（12 管 + 2 块弓形折流板 + 蛇形绕流）+ 端面 + 三个版本对照 |
| `pin-fin.svg` | 针翅散热器：俯视（10×4 针阵）+ 纵剖面（针高 20、上方 10 旁通）+ 端面 + 实测 |
| `toolkit-report.html` | **能力汇报页**：能力矩阵、实测基线、8 个示例、15 条踩坑记录、仓库状态 |

这些图是脚本直接生成 SVG（不依赖 matplotlib 之类），所以在任何浏览器里都是矢量清晰。

```powershell
# 想重新生成（图里的数字是硬编码的实测值，改模型后要同步）
python ..\..\<生成脚本>
```

汇报页 `toolkit-report.html` 是自包含的单文件，双击就能看。
